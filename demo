import axios from "axios";

const API_URL = "http://localhost:8080/api/check";

class NumberService {
  checkNumber(num) {
    return axios.get(`${API_URL}/${num}`);
  }
}

export default new NumberService();