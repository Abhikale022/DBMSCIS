import axios from "axios";

const API_URL = "http://localhost:8080/api/student";

export const getReportCard = async (student) => {
  try {
    const response = await axios.post(`${API_URL}/report`, student);
    return response;
  } catch (error) {
    return error.response; // return error response for status 406
  }
};